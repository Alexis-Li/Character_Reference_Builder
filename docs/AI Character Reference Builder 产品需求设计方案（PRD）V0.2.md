# AI Character Reference Builder 产品需求设计方案

**文档版本：** V0.2  
**文档阶段：** 正式立项前需求评审稿  
**产品定位：** 面向 3D 角色／服装模型师的节点式参考资料生产工具

## 1. 产品目标与范围

产品将角色原画整理、补全为可供 3D 建模使用的参考资料。

主要使用任务是将立绘整理为保持设计一致性的 A-pose，并逐个生成、优化部件多角度图。模型师可以将选定图片交给外部 3D 生成工具，或用于后续人工建模。产品交付参考图片，首版不承担外部 3D 生成服务的自动接入。

核心目标：

- 保持原画已经明确的设计。
- 补齐姿态、视角、遮挡及部件结构信息。
- 支持模型师审核、纠错和局部返工。
- 支持在生成结果后继续连接优化节点，通过多轮修改形成可用参考。
- 保留参考来源、推测范围和人工审核状态。

优先级为：**保持原设计 > 补足建模信息 > 画面美化。**

首版面向个人工作的人形角色及服装模型师，覆盖写实和风格化角色、服装、鞋、护甲及穿戴附件。头部、头发可以作为部件组织，但首版不提供专门的毛发制作或面部拓扑流程。小团队通过导出资料交接，不包含多人同时编辑。

所有 AI 推理使用云端。普通查看、框选、标注、裁切和排版不属于模型推理。

不纳入首版：

- 非人形生物、复杂机械角色的专用流程；
- 本地模型或本地 GPU 推理；
- 自动生产级建模、拓扑、UV、绑定及动画；
- 工业纸样、服装物理仿真或精确尺寸反推；
- DCC 实时同步；
- 通用绘图社区、模型训练及素材市场。

交付物是建模参考，不是精确测量图或最终 3D 资产。

## 2. 典型用户场景

| 场景 | 用户路径 |
| --- | --- |
| 只有正面或 3/4 原画 | 确认设计 → A-pose → 多视图 → 拆件 → 指定部件 → 生成参考 |
| 已有合格 A-pose | 直接使用，不强制重新生成 |
| 已有部分多视图 | 使用已有设计依据，只补缺失信息 |
| 只需要一个部件 | 框选或文字指定 → 确认目标 → GPT Image 生成参考 |
| 部件存在遮挡 | 根据原画和已有视图补全，人工确认推测内容 |
| 需要精确边界或透明提取图 | 指定部件 → SAM 分割 → 人工修整 → 提取或继续生成 |
| 修改原画或替换候选 | 查看受影响结果 → 局部返工，保留无关结果 |
| 部件多视图还原程度不足 | 连接参考优化节点 → 补充修改要求 → 修改选定视图或整组 → 比较选择 |
| 需要尝试不同修正方式 | 从同一结果分支连接优化节点 → 分别生成 → 选用满意版本 |

仅需要部件参考时，不要求先生成 Mask，也不要求配置 SAM 服务。

## 3. 设计依据与人工控制

### 3.1 资料身份

资料必须区分：

- 原画依据；
- 用户补充说明或明确的设计更正；
- 有指定用途的辅助参考；
- 未确认的 AI 推测；
- 已确认、可用于建模的 AI 推测。

AI 分析结果仍属于识别结果，不自动成为原画事实。

### 3.2 冲突与约束

多张正式资料冲突时，指出具体差异，由用户确认采用的依据。辅助风格参考不能自动改变结构、数量或左右关系。

用户可确认需要保持的轮廓、比例、配色、图案、数量、层级、连接及左右关系。这些约束进入后续生成与检查，但不承诺模型绝对遵守。

普通任务提示词不能静默推翻已确认约束；明确更正设计时，更新依据并提示受影响结果。

### 3.3 推测

缺失设计允许推测，经人工确认后可继续使用，但始终保留推测来源。无法准确定位推测区域时，明确标注整张参考包含推测。

## 4. 默认工作流与可选分支

### 4.1 默认完整流程

**资料输入 → 原画理解 → 设计依据确认 → A-pose → 多视图 → 建模拆件 → 框选／文字指定部件 → GPT Image 部件参考生成 → 一致性检查 → 人工审核选择 → 输出。**

A-pose、角色多视图和部件参考结果后均可继续连接参考优化节点。用户可以直接提出修改要求，也可以根据一致性检查的问题继续优化；由用户决定何时选定结果用于后续生产或导出。

### 4.2 单部件快捷流程

**原画及必要说明 → 框选／文字指定部件 → GPT Image 部件参考生成 → 检查与选择 → 输出。**

无需先生成角色 A-pose、多视图或完整拆件清单，但需要明确目标部件及其设计依据。

### 4.3 可选精细分割流程

**选定图片与部件 → SAM 精细分割 → 遮罩确认／修整 → 精确提取或导出。**

分割结果也可以作为 GPT Image 生成任务的辅助参考。双方职责为：

- GPT 负责理解设计、识别目标、补全结构和生成参考。
- SAM 只负责指定图片中的精细分割。

只有需要像素级 Mask、透明提取图或明确边界提取时，才由用户插入 SAM。生成失败、部件遮挡或画面复杂，不自动触发 SAM，也不自动产生分割费用。

已有合格资料可以替代生成阶段；已有合格遮罩可以替代 SAM。跳过阶段后缺少必要信息时，提示补充，不虚构输入。

### 4.4 连续优化与分支

**生成结果 → 参考优化 → 查看或检查 → 继续优化／分支比较 → 人工选择 → 输出。**

优化节点可以连续连接，也可以从同一候选分出不同优化方向。未批准的候选可以作为优化输入，检查报告不是继续优化的前置条件。

用户选择需要修改的单个或多个视图，其他满意视图作为一致性参考；也可以明确选择整组优化。每轮产生可比较的新候选，原结果保留。

## 5. 节点体系

| 节点 | 职责与结果 |
| --- | --- |
| 资料输入 | 导入原画和补充资料，指定角色、视角及用途 |
| 原画理解 | 识别比例、服装层级、部件关系、对称性及不确定项 |
| 设计依据确认 | 人工确认设计约束、资料冲突及允许推测范围 |
| 姿态整理／A-pose | 生成并审核适合建模观察的姿态参考 |
| 多视图补全 | 使用已有视图，补齐必要视角并组织对照 |
| 建模拆件规划 | 确定制作单元、连接关系和各部件所需参考 |
| 部件指定 | 通过框选、文字或二者结合明确生成目标 |
| 部件参考生成 | GPT Image 根据原画上下文和目标要求生成独立参考 |
| 参考优化 | 继承原画、当前结果及约束，按修改要求优化选定视图或整组，支持连续连接和分支 |
| 精细分割／SAM，可选 | 生成可见区域 Mask，支持确认、修整和提取 |
| 一致性检查 | 输出具体设计差异、依据和无法判断项 |
| 审核与选择 | 比较、选中、批准、驳回或接受已知问题 |
| 输出 | 检查交付完整性，导出角色或指定部件参考包 |

自然语言要求支持项目级、部件级和单任务级作用范围，并可保存常用要求。

设计锁定、对称属性和参考来源是贯穿任务的约束，不要求用户额外搭建必经节点。节点代表实际生产任务，不暴露采样器、VAE 等底层模型参数。

## 6. 部件指定与 GPT Image 直接生成

### 6.1 指定方式

用户可以：

- 框选目标，例如肩甲或腰带；
- 文字指定，例如“角色左肩的金属肩甲”；
- 框选并补充说明，例如“只处理框中的腰带，不包含手和挂件”。

支持使用带红框等标记的完整设计图和文字说明指定部件，也支持补充局部截图。框线属于目标指示，不属于角色设计，不应出现在生成的部件上。

不强制文字指定转成框选，也不强制先生成遮罩。

出现多个同名部件、左右歧义或范围不明确时，展示理解结果并请用户确认。不能可靠定位时，请求更具体的说明或框选，不默认转交 SAM。

框选主要表达目标范围，不等于精确裁切边界，也不表示允许丢弃框外设计上下文。

### 6.2 生成输入

部件参考生成必须使用：

- 原画或用户选定的设计参考；
- 目标部件的框选、文字说明或二者；
- 已确认的设计约束；
- 与该部件相关的角色、层级及连接信息；
- 用户要求的参考表达方式。

已有多视图、细节图、裁切图和 Mask 均为按需使用的辅助资料，不是固定必填项。

用户不需要为每个部件重新描述整个角色。

不同部件各自维护生成与优化上下文，共享原画及已确认的共同设计约束，不自动混入其他部件的候选和纠错历史。同一部件的连续优化保留当前有效的修改要求；用户更正旧要求时，以明确更正后的要求为准，并可回看历史。

### 6.3 生成结果

支持三类参考：

- **服装参考：** 保持穿戴形态的必要视图。
- **物件参考：** 鞋、护甲、腰带、挂件等的独立视图。
- **细节参考：** 衣领、扣件、接缝及连接位置等局部表达。

默认保持穿戴或装配形态。服装不自动变成平铺纸样，独立展示不丢失佩戴方向及连接位置。

只生成建模所需信息，不要求所有部件都生成完整多视图。已有足够资料的细节可以直接放大查看，不强制重新生成。

GPT Image 输出属于生成参考，不是对原图像素的精确提取，不能将其当成保持原始边界和纹理像素不变的资产。

### 6.4 遮挡与补全

已有其他视图时优先利用其设计依据；没有依据时允许生成推测。

简单补全可以在同一次部件参考生成中完成，并明确标记推测。涉及重要连接、层数或不可见结构的设计选择时，先确认补全方案，再继续生成。

用户可以分别返工补全方案和最终参考表达。SAM 不承担遮挡恢复或不可见结构推断。

缺陷修正应针对指定部件、视图或区域。不能可靠完成局部编辑时，允许生成替代候选或导入外部修正版，再重新检查。

### 6.5 参考优化

参考优化节点接收原画依据、用户选定的当前结果、当前有效约束和本轮修改要求。用户可指定需要保持的其他视图，并选择单个视图、多个视图或整组作为修改范围。

- 只生成所选修改范围的新候选；未选视图沿用原图，不重新生成或替换。
- 未选视图作为一致性参考时，仍可能需要云端读取，运行前说明输入范围及可获得的费用估计。
- 修改要求可以是恢复原画轮廓、修正装饰、调整姿态或解决视图间矛盾，不要求用户先取得检查报告或 Mask。
- 每个新候选可继续连接优化节点，也可返回任意已有候选另开分支。
- 优化失败或效果不满意时保留原结果，不自动扩大为整组重生成。
- 如果所选修改会与保留视图产生冲突，说明冲突，由用户决定调整要求或扩大修改范围。

对照检查应覆盖修改结果与原画，以及修改结果与保留视图的关系。其他满意视图是参考约束，不保证模型一次遵守；偏差可以继续通过后续节点修正。

## 7. SAM 可选精修要求

SAM 适用于：

- 导出像素级部件 Mask；
- 从指定图片提取透明部件图；
- 明确排除相邻物体，修整目标边界；
- 为后续支持相应能力的处理提供遮罩或提取参考。

SAM 不适用于判断部件如何建模、补全被遮挡结构、生成新视角或自动修正 GPT 生成的设计偏差。

节点支持框、正负点、多候选遮罩选择、简单增删修整，以及导入外部修正遮罩。一个部件可以对应多个不连通的可见区域。

遮罩绑定具体图片版本和部件，不能直接跨视图或跨生成版本使用。对生成图进行分割，只能提取该生成图已有内容，不能借此证明其忠于原画。

分割失败时允许修整、换图或放弃分割，保留已有候选。未依赖该遮罩的生成和导出路径继续可用。

传入 Mask 不代表 GPT Image 必然支持严格遮罩编辑；若当前调用方式不支持，明确说明，只按实际支持的辅助参考方式使用。

## 8. A-pose、多视图与拆件

### 8.1 A-pose

默认保守保持设计。用户可以提高姿态标准化要求，但不得静默改变服装结构。

重点审核比例、服装长度、数量、图案、装饰位置、层级和左右关系。无法消除的遮挡应明确说明，不任意挪动穿戴部件来制造无遮挡效果。

### 8.2 多视图

默认正、侧、背三视图，可增加前后 45° 及另一侧。

- 左右使用角色自身方向，并注明侧面所展示的一侧。
- 非对称设计不得默认镜像。
- 缺少另一侧关键结构时提示补充。
- 保持可比较的姿态、比例、尺度和地面基线。
- 支持人工排列与对齐，不通过拉伸形体伪造一致。
- 使用便于读结构的背景与光照。
- 可单独重做某个视图。
- 保留独立视图原图，总览图不替代单视图细节。

目标是便于建模观察的近正交参考，不承诺二维生成获得真实几何一致的正交投影。

### 8.3 拆件

原画理解负责识别结构；建模拆件规划负责制作单元和参考任务。

每个部件明确名称、归属、数量、左右、覆盖、连接关系、可用参考及所需输出。父子归属、覆盖和固定关系分别表达。

用户可新增、删除、拆分、合并和重命名，只生成所选部件，不默认批量生成全部拆件。部件变化后重新确认已有资料归属，不自动删除资产。

## 9. 一致性检查与人工审核

检查三个层面：

1. 生成结果与原画及确认设计是否一致。
2. 同一部件不同视图是否矛盾。
3. 独立部件与角色整体的比例、连接和层级是否冲突。

检查数量、轮廓、比例、左右、配色、图案、材质观感、覆盖和连接。

每条问题包含涉及的图片或部件、比较依据、具体差异及是否影响建模。无法判断时明确保留不确定性。

不以单一评分表示通过，也不把未发现问题视为质量保证。

用户可选中、批准、驳回、纠正检查判断或带说明接受已知问题。选中候选不等于批准。

默认在设计依据、A-pose、多视图、拆件和部件输出阶段提供人工确认位置；使用 SAM 时增加遮罩确认。用户可先连接优化节点继续修改候选，满意后再确认用于后续生产。无需每次额外放置审批节点。

一致性检查为用户提供修改线索，可在中间阶段重复使用，也可将问题带入优化节点。AI 检查不决定迭代次数，不要求首轮通过，由用户决定继续优化、选用已有结果或带说明导出。

## 10. 执行、版本与失败恢复

画布支持连接、分支、预览、单任务执行、局部重跑、禁用、节点组和保存。连接需满足实际输入要求，不兼容时说明原因。

- 显示排队、执行中、等待确认、完成、失败、取消和结果状态未知。
- 重跑产生新候选，保留已有选择。
- 连接优化节点产生独立的后续候选，不改写其输入版本；支持回看优化链、比较分支及选择结果。
- 上游修改后标记受影响结果待复核，不自动覆盖或付费重跑。
- 不相关分支可以继续执行，失败不清空项目。
- SAM 不可用只影响需要分割的任务。
- 关闭重开后恢复资料、选择及审核状态，不默认重新提交请求。
- 断网时仍可查看已保存资料、标注和导出。
- 支持暂停后续任务和取消；不承诺取消能撤销已发生的云端费用。

请求可能已经执行时，优先确认状态。无法确认则提示可能重复消费，由用户决定是否重试，不自动切换服务重复提交。

## 11. Provider 与费用

### 11.1 默认策略

ChatGPT OAuth 用于日常理解与生成，OpenAI API 作为补充。SAM 为独立可选云服务。

| 模式 | 行为 |
| --- | --- |
| Prefer OAuth，默认 | 优先订阅路径，不可用时提供 API 补充 |
| OAuth Only | OpenAI 任务只使用订阅路径，不可用时暂停 |
| API Only | OpenAI 任务使用按量付费 API |

节点继承项目模式，允许显式覆盖。切换不能要求重搭工作流或重新描述角色。

默认完整流程不要求 SAM 账号。用户插入 SAM 时才检查其服务配置并提示费用。OAuth Only 不限制用户显式使用 SAM，但不能将其费用标为订阅内消费。

### 11.2 登录与能力

支持登录、退出、重新认证及账号切换。认证失效保留项目，正常使用不要求重复登录。

区分登录成功、模型可用、任务能力支持和额度状态。未知额度或恢复时间如实标记，不承诺所有订阅能力一致或无限使用。

生成、参考图编辑及严格遮罩编辑分别判断，不能互相推定。

凭据不进入项目、模板、导出或可分享错误记录。

### 11.3 回退与付费

默认确认后切换 API；用户可以预先开启有明确预算上限的自动补充，无须逐节点重复授权。

显示实际 Provider、模型、付费方式、切换原因及能力差异；无法确认的信息明确标记。

不得静默付费、丢弃参考和约束、无限重试，或因内容拒绝、输入错误、质量不满意自动跨服务重复请求。

运行前展示本次范围、预计调用数量及可获得的费用估计。估计与实际账单分开；不能可靠控制预算时暂停自动付费执行。

## 12. 项目保存与交互

项目保存原画、说明、约束、部件关系、视图、生成候选、选择、审核意见和来源。遮罩及分割记录只在实际使用时存在。

同时保存部件各自的有效修改要求、优化节点连接、分支、每轮修改范围及保留视图，重新打开后可以从选定候选继续优化。

任意结果应能追溯：

- 所属部件与视角；
- 使用的设计依据；
- 推测内容；
- 上游版本；
- 审核状态及是否待复核。

项目保存和模板保存分开。分享模板默认不包含原画、私人说明或凭据。

节点画布提供部件导航、预览、状态和并排比较，支持同步缩放、原始细节查看及外部修正版导入。

云端处理前明确发送的资料和目标服务，不默认向每个任务发送整个项目。有效结果不能仅依赖可能失效的远程预览链接。

## 13. 导出

支持导出已审核角色参考、指定部件或全部候选过程资料。

默认导出当前有效、已审核的结果，包括：

- 独立视图图片；
- 参考总览；
- 名称、视角、左右及必要连接说明；
- 推测范围、审核状态和已知问题；
- 实际存在且用户选择导出的 Mask、透明提取图。

未运行 SAM、没有 Mask，不构成默认参考包缺失。需要精确提取的特定交付任务，才检查对应遮罩或透明图是否齐备。

保留可用原始分辨率，说明不遮挡关键结构；放大图片不冒充新增真实细节。

导出前检查所选交付要求、归属和审核状态。允许显式导出未完成资料，但保留状态标记。

导出参考包用于建模交接；可继续编辑的项目另行保存。

## 14. MVP 与复用范围

### 14.1 MVP 必须闭合

- 资料输入、用途及冲突确认；
- 设计理解与人工约束；
- A-pose、默认三视图和必要补角度；
- 可编辑拆件；
- 框选／文字指定部件；
- 无 Mask 前置要求的 GPT Image 直接生成；
- 可连续连接的参考优化节点，支持选定视图优化、整组优化、分支比较和历史结果保留；
- 部件独立上下文及共同设计约束继承；
- 必要遮挡补全及推测确认；
- 衣物、物件、细节参考；
- 检查、选择、局部返工、保存与导出；
- OAuth 主用、API 补充及费用控制。

SAM 为首版提供的可选精修能力，不是每个项目的必经步骤或默认流程验收前提。分割功能单独验收，只有使用它的任务依赖相关服务。

预设入口为完整角色、仅多视图、单部件参考及精确部件提取；前三者默认不包含 SAM。

延后六／八视图、连续相机路径、自动镜像整套部件、无人值守全量生成、复杂通用编排、专业绘画编辑器及多人协作。

### 14.2 复用原则

优先复用通用画布、标注、图片比较、保存和云端能力；产品重点定义设计依据、拆件、上下文继承及一致性审核。

第三方能力的评估结果见附录。候选不等于已选定底座，公开功能声明不等于已完成本产品验证。

## 15. 验收与立项条件

### 15.1 必验场景

| 场景 | 通过要求 |
| --- | --- |
| 单张非标准姿态原画 | 可得到人工接受的 A-pose 和基础视图，已有设计变化可被发现和处理 |
| 只框选一个部件 | 无 SAM、无 Mask，也能生成、审核并导出参考 |
| 只用文字指定部件 | 能正确确认目标；存在歧义时允许澄清 |
| 框中包含手和腰带 | GPT 理解要生成的部件，不要求先分割 |
| 被遮挡的部件 | 利用已有依据补全，推测明确可审 |
| 明确要求像素级提取 | 用户插入 SAM，得到可修整和导出的遮罩或透明图 |
| SAM 未配置或失效 | 默认部件生成路径仍完整可用 |
| 对 GPT 结果不满意 | 可直接连接优化节点继续修正，未批准或未经过 AI 检查的候选也可作为输入 |
| 正面满意、背面偏离原画 | 只优化背面，以正面及原画为参考；正面原图保留，不重新生成或替换 |
| 整组视图需要调整 | 用户明确选择整组优化，产生新候选，保留此前整组结果 |
| 多轮优化或分支尝试 | 有效修改要求持续继承，支持从历史候选分支、比较和选用 |
| 修改与保留视图冲突 | 说明冲突，由用户决定是否扩大范围，不自动重生成其他视图 |
| 连续处理不同部件 | 共享原画和共同约束，各部件的候选及纠错历史独立 |
| 已有背面与非对称设计 | 实际使用设计依据，保持左右和数量 |
| 多张原画冲突 | 用户可明确处理冲突，不自动混合成新设计 |
| 上游修改、重跑和重开 | 保留历史选择，关联结果待复核，无关结果不重跑 |
| OAuth 失效或请求状态未知 | 可恢复或明确回退，不自动重复消费 |
| 检查漏报或误报 | 用户可修正判断，检查不代替最终审核 |
| 默认参考包导出 | 没有 Mask 仍可完整交付，来源和审核状态齐全 |

### 15.2 成功判断

验收采用真实角色资料，覆盖从首次生成、连续优化到最终选择的完整过程，由模型师判断能否继续工作，不能只挑选展示效果好的样图。

- 模型师可将选定部件多角度图交给外部 3D 生成工具或用于人工建模，不必重复解释角色设计。
- 同等输入与交付要求下，人工整理、纠错及交接耗时降低；模型等待时间单列。
- 正式输出均有身份、来源和审核状态。
- 不存在未知的自动付费回退或重复提交。
- 固定代表性任务中，OAuth 承担过半理解与生成任务，并单独报告图像任务覆盖。
- 将返工、等待和补充费用纳入评价。
- 以获得满意参考所需的总操作负担和总成本判断价值，不以单次生成通过作为必要条件。

### 15.3 立项验证

立项前验证实际账号下的 OAuth 图像生成与必要编辑能力、日常额度和使用条件，以及代表性角色参考质量。不能用 API 跑通代替 OAuth 主路径验证。

若 OAuth 无法承担日常主路径，应视为当前方向的立项阻塞，不能默认改成 API 主用后宣布验证通过。

SAM 的质量与费用用于评价可选精确提取能力，不作为 GPT 直接生成主流程成立的必要条件。

本 PRD 不确定代码、框架、接口、存储、目录结构或开发排期。

## 附录：第三方候选核查

**核查日期：** 2026-09-09  
**证据范围：** 公开项目说明、发布记录、许可证及相关源文件。未进行账号登录、真实模型调用或完整用户路径测试。

| 候选 | 核查结果 | 复用判断 |
| --- | --- | --- |
| Node Banana — shrimbly/node-banana | 已有节点工作流、Prompt、标注、比较、Gallery 和保存加载；仓库声明 MIT。v1.7.0 发布记录已包含 OpenAI 图像生成与编辑，README 的 Provider 表与之存在漂移 | 通用功能可优先直接复用；完整底座需二次开发评估，尤其是部件归属、审核、版本及失败恢复。[仓库](https://github.com/shrimbly/node-banana)、[发布记录](https://github.com/shrimbly/node-banana/releases) |
| openai-oauth — EvanZhouDev/openai-oauth | 提供 ChatGPT 登录、图像生成和编辑；Apache-2.0。项目明确不支持图像编辑遮罩等部分能力；托管网页登录方案涉及浏览器扩展 | 可二次开发，作为优先评估候选；不能据此承诺严格遮罩编辑、日常额度或长期稳定性。[项目说明](https://github.com/EvanZhouDev/openai-oauth)、[许可证](https://raw.githubusercontent.com/EvanZhouDev/openai-oauth/main/LICENSE) |
| chatgpt-bridge — l0z4n0-a1/chatgpt-bridge | 项目声明 MIT，通过 ChatGPT OAuth 提供兼容代理和图像生成；存在多个同名不同项目 | 可二次开发，作为备选候选；不建议作为唯一保障。与其他 OAuth 桥接候选共享上游，不能视为独立可用性保障。[项目说明](https://github.com/l0z4n0-a1/chatgpt-bridge) |
| fal.ai SAM 3 | 服务说明列出文本、正负点、框、多遮罩和边界框；当前图像入口输入列表未列初始遮罩 | 可直接采用的可选云服务候选，不是开源底座；具体能力以该服务入口为准。[服务文档](https://fal.ai/models/fal-ai/sam-3/image/api) |
| Meta SAM3 | 官方仓库提供模型运行、微调代码和权重获取说明 | 参考模型能力边界，不作为本地运行依赖；模型与托管服务分开评估。[官方仓库](https://github.com/facebookresearch/sam3) |
| ComfyUI-SAM3 — PozzettiAndrea/ComfyUI-SAM3 | 提供 ComfyUI 分割封装及交互相关能力，另有多个近似名称项目 | 只参考点选、遮罩和预览设计，不引入本地 ComfyUI 推理链。[仓库](https://github.com/PozzettiAndrea/ComfyUI-SAM3) |
| ComfyUI-H3-ContactSheet — matlowai | 提供依赖特定模型及配套条件的五视图流程 | 只参考视角组织，不能作为 OpenAI 云端模型达到同样一致性的证据。[仓库](https://github.com/matlowai/ComfyUI-H3-ContactSheet) |
| ComfyUI-MiniMax-H3-Edit — ethanfel | 包含 Character Sheet、服装视图和连续相机视角逻辑 | 只参考设计，不迁移特定模型的效果保证。[项目说明](https://github.com/ethanfel/ComfyUI-MiniMax-H3-Edit) |
| opencode-openai-codex-auth | 提供 OAuth、刷新和错误处理，使用说明明确面向个人编程辅助 | 只参考认证体验，不建议作为本产品运行依赖。[使用说明](https://github.com/numman-ali/opencode-openai-codex-auth/blob/main/docs/getting-started.md) |
| Hermes Agent | 项目文档描述 Codex OAuth、凭据导入、失效处理及 OAuth 图像生成能力 | 参考能力组织和认证体验；不为单一调用能力引入完整 Agent 产品。[Provider 文档](https://hermes-agent.nousresearch.com/docs/integrations/providers/)、[内置能力](https://hermes-agent.nousresearch.com/docs/user-guide/features/built-in-plugins) |

OpenAI 官方文档确认 Codex 支持 ChatGPT 订阅登录与 API 按量使用两种方式，但这不自动证明第三方应用具备完整、长期稳定的图像生产能力。[OpenAI 认证说明](https://learn.chatgpt.com/docs/auth)
