# Character Reference Builder

面向 3D 角色与服装建模的参考资料生产领域。以下词汇取自当前产品草案，用于统一沟通，不代表功能已实现。

## Language

**角色项目（Character Project）**：围绕同一角色设计组织的原画、工作流、结构信息、部件和参考结果的完整集合。
_Avoid_：将单张图片或一次生成任务称为角色项目。

**原画依据（Design Reference）**：用户提供、用于说明已有角色设计的原始图片及补充资料；多张资料可以共同描述同一角色。
_Avoid_：将 AI 推测生成的内容称为原画事实。

**设计信息（Design Data）**：角色比例、服装层级、结构关系和视觉特征的结构化描述。

**建模部件（Part）**：从 3D 制作角度划分的角色或服装单元，可包含子部件以及层级、覆盖、连接或对称关系。
_Avoid_：将部件等同于任意裁切区域或图像语义类别。

**拆件方案（Part Plan）**：针对建模部件的划分和参考生成需求提出的可人工调整方案。

**目标区域（Region）**：在某张参考图上指明目标部件位置的空间标记。
_Avoid_：与精确遮罩混用。

**部件遮罩（Mask）**：标明图片中哪些像素属于目标部件的选区信息。

**A-pose 参考（A-pose Reference）**：以适合建模观察的 A-pose 表达角色、同时保持原设计信息的参考图。

**多视图（View Set）**：描述同一角色或部件不同观察角度的一组相互关联的参考图。
_Avoid_：将任意图片集合称为多视图。

**部件参考（Part Reference）**：面向某个建模部件的独立视图、结构或细节资料，继承所属角色的设计上下文。

**设计锁定（Reference Lock）**：后续生成需要保持的轮廓、配色、数量、比例和结构关系等设计约束。

**推测内容（Inferred Content）**：原画未明确展示、由 AI 补全的设计信息。
_Avoid_：将推测内容表述为已确认的原始设计。

**一致性检查结果（Review Result）**：对原画与生成结果、或多个视图之间具体设计偏差的记录。
_Avoid_：画面美观评分。

**人工选定结果（Selected Result）**：用户从候选参考中选中、用于后续处理或输出的结果。
_Avoid_：将所有生成结果默认视为人工认可。

**资产清单（Asset Manifest）**：描述参考资产与角色、部件、视角及生成记录之间归属关系的清单。
